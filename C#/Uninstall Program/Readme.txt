This program uses the Win32_Product class to uninstall programs. It accepts a command line argument of the program you wish to uninstall. If the argument contains spaces in the string, wrap it in quotes (Example: "Program to search for". It searches for program names that contain the argument passed, and not an exact match.

Example:
.\UninstallProgram.exe "Adobe Flash"
This will uninstall Adobe Flash Player ##